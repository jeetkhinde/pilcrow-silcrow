# Experimental Baked Pages (Removed)

This feature has been removed. The `experimental-baked-pages` Cargo feature and the
`baked_pages/` module no longer exist in the codebase.

Use FSR (`PROMOTE_AFTER`) for durable page serving. See [[../03 Rendering/Rendering Models]]
and [[Removed and Legacy Features]].
